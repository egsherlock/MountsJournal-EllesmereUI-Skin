Screenshots used by the main README, and handy for the CurseForge gallery too.

  mounts.png          the mount list, journal open on the Model tab
  grid.png            the grid view, showing the mounts per row slider
  settings.png        Settings > Main
  class-settings.png  Settings > Class settings

If you replace any of them keep the same filenames, so the README keeps
working, and keep them around 1000 to 1400px wide. This folder is repo only;
.pkgmeta keeps it out of the release zip.
